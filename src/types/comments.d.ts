type TCommentForm = {
    body: string
    name: string
    postId: number
    email: string
    id?: number
}

type TComment = {
    id: number;
    name: string;
    postId: number;
    email: string;
    body: string;
}